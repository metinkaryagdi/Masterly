<<<<<<< HEAD
name: CI
=======
﻿name: CI
>>>>>>> f2458aa (refactor: rename project to CodeCraft.NET (CodeCraftNet))

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
<<<<<<< HEAD
  workflow_dispatch:

jobs:
  test:
    name: Build & test (.NET 8)
    runs-on: ubuntu-latest

=======

env:
  DOTNET_NOLOGO: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true

jobs:
  build-and-test:
    runs-on: ubuntu-latest
>>>>>>> f2458aa (refactor: rename project to CodeCraft.NET (CodeCraftNet))
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
<<<<<<< HEAD
          dotnet-version: '8.0.x'

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
          restore-keys: ${{ runner.os }}-nuget-

      # The solution file is in the .slnx format, which only the .NET 9+ SDK can
      # read, so the test projects are built directly — they pull in the rest of
      # the solution through project references. Integration tests run on
      # WebApplicationFactory + in-memory Sqlite, so no PostgreSQL or Docker is
      # required in CI.

      - name: Unit tests
        run: >
          dotnet test tests/TrainingPlatform.UnitTests/TrainingPlatform.UnitTests.csproj
          --configuration Release --verbosity normal

      - name: Integration tests
        run: >
          dotnet test tests/TrainingPlatform.IntegrationTests/TrainingPlatform.IntegrationTests.csproj
          --configuration Release --verbosity normal
=======
          dotnet-version: "8.0.x"

      - name: Restore
        run: dotnet restore CodeCraftNet.slnx

      - name: Build
        run: dotnet build CodeCraftNet.slnx --no-restore -c Release

      - name: Unit tests
        run: dotnet test tests/CodeCraftNet.UnitTests/CodeCraftNet.UnitTests.csproj --no-build -c Release --logger "trx;LogFileName=unit-tests.trx"

      - name: Integration tests
        run: dotnet test tests/CodeCraftNet.IntegrationTests/CodeCraftNet.IntegrationTests.csproj --no-build -c Release --logger "trx;LogFileName=integration-tests.trx"

      - name: Publish test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: "**/*.trx"

  docker-build:
    runs-on: ubuntu-latest
    needs: build-and-test
    steps:
      - uses: actions/checkout@v4

      - name: Build api image
        run: docker build -f src/CodeCraftNet.Api/Dockerfile -t codecraftnet-api:ci .

      - name: Build runner image
        run: docker build -f src/CodeCraftNet.Runner/Dockerfile -t codecraftnet-runner:ci .
>>>>>>> f2458aa (refactor: rename project to CodeCraft.NET (CodeCraftNet))
